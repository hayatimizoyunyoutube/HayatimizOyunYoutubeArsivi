# v2.5.2 FIX4 - Schema UUID Bildirim Fix

- Supabase `site_notifications.id` kolonu UUID tipindeyken schema içinde metin id (`v252-release`) gönderilmesi düzeltildi.
- Bildirim başlangıç kaydı artık geçerli UUID ile eklenir.
- `site_notifications` tablosu yeni kurulumlarda UUID id ile oluşturulur.
- Eski tabloda id zaten UUID ise hata vermez.
- Schema sonucu `v2.5.2 FIX4` olarak güncellendi.
