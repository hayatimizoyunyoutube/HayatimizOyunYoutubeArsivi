# v2.4.8 FIX1 - Schema site_schema_versions Düzeltmesi

- `site_schema_versions` tablosunda eksik `note` kolonu nedeniyle oluşan SQL hatası düzeltildi.
- `schema.sql` artık `note`, `version`, `created_at`, `updated_at` kolonlarını güvenli şekilde ekler.
- Sürüm notu kaydı unique constraint gerektirmeden sil-yeniden ekle mantığıyla çalışır.
