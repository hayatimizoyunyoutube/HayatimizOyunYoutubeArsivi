# Hayatımız Oyun v2.1.3 Fix 14

Bu paket Supabase `ON CONFLICT` constraint hatasını düzeltir ve temiz kurulum sırasını netleştirir.

Önce `KURULUM-KOMUTLARI.txt` dosyasını oku.

En önemli sıra:
1. Supabase SQL
2. `.git` silinmeden temiz klasör kurulumu
3. GitHub force push
4. Vercel Redeploy + Clear Build Cache
