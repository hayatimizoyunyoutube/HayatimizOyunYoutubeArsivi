# Git Kodları — v2.3.0

```bash
git add -A
git commit --allow-empty -m "v2.3.0 premium seri sayfasi arsiv deneyimi"
git push -f origin main
```

Vercel:

```text
Deployments > Redeploy > Clear Build Cache > Redeploy
```
