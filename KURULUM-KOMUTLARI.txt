Hayatımız Oyun v2.1.3 Site Yükleniyor + Schema Fix

1) Supabase SQL Editor içinde sadece şunu çalıştır:
   supabase/schema.sql

2) Yetki gerekirse:
   supabase/YETKI-ORNEK-SQL-v213.sql

3) Bilgisayarda proje klasöründe .git silinmeden temiz kurulum yap:
   01-siteyi-temizle-git-ve-bat-haric.bat

4) Bu ZIP içindeki dosyaları proje klasörüne çıkar.

5) GitHub'a gönder:
   02-githuba-otomatik-gonder.bat

6) Vercel:
   Redeploy > Clear Build Cache

Not: Bu fixte vercel.json içindeki genel catch-all rewrite kaldırıldı. Bu hata assets JS/CSS dosyalarını index.html'e yönlendirip ekranda sadece "Site yükleniyor..." kalmasına neden olabiliyordu.
